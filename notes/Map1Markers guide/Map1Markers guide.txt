Map1 Markers — How It Works


Table of Contents

1. Quick overview
2. Card pool vs player collection
3. Folder layout
4. Map 1 markers (this folder)
5. Card rewards on markers (first clear)
6. Room setup (Room_Worldmap1)
7. Battlesets (which enemies spawn)
8. Adding a new map 1 marker
9. Starting map 2 (Map2Markers folder)
10. Adding new monster JSON


1. Quick overview

World map events are battle markers placed in the room.
Each marker object knows:
  - unlock order
  - display label
  - which battle to run (battle01, battle02, ...)
  - which battleset JSON file to read
  - replay battles after the event is cleared
  - optional card reward on first clear (fixed id or random pool)

Flow when the player presses E at a marker:

  OBJ_Map1_MarkerXX  (Create event config)
        ↓
  worldmap_SyncMarkersFromRoom()  (on map load)
        ↓
  worldmap_LaunchEventBattle()
        ↓
  Grasslands_Battleset01_starter.json  (waves / enemy IDs)
        ↓
  EnemyCollection01.json  (enemy stats, abilities, animation)
        ↓
  Room_battle
        ↓
  First clear → card added to player collection


2. Card pool vs player collection

Two separate layers:

  card_DB (card pool)
    - Every card definition loaded from JSON (Merc_starterdeck01.json, etc.)
    - Loaded at game start by SCR_LoadAllCollections()
    - Cards WITHOUT "own" exist in the pool only — not given to the player at start

  global.player_collection (player collection)
    - Cards the player actually owns (owned count per card)
    - Deckbuilder only shows cards from here
    - Starting cards: JSON field "own" on a card (granted once at first boot)
    - Earned cards: map marker rewards, future traits/saves, etc.

Example — Advance Strike (id 8) has no "own" in JSON, so it is pool-only until
Marker01 grants it on first clear.


3. Folder layout

Objects / WorldMapSystem
  EventMarkers
    OBJ_EventMarker          ← parent object (logic only — do not place in room)
  Map1Markers                ← YOU ARE HERE
    OBJ_Map1_Marker01
    OBJ_Map1_Marker02
    OBJ_Map1_Marker03
    OBJ_Map1_Marker04
    OBJ_Map1_Marker05
  Map2Markers
    OBJ_Map2_Marker01        ← template for a second map


4. Map 1 markers (this folder)

Each marker is a child of OBJ_EventMarker.
Battle settings live in that object's Create event.

Example — OBJ_Map1_Marker01 / Create:

  event_inherited();
  eventmarker_apply_config(
      1,                                              // marker_order (unlock sequence)
      "Trail Start",                                  // label shown on map
      "battle01",                                     // battle id in battleset JSON
      "Grasslands_Battleset01_starter.json",          // battleset file
      "battle01,battle02,battle03,battle04,battle05" // replay pool when cleared
  );

Parameters for eventmarker_apply_config:
  1. marker_order     — 1 = first event, 2 = second, etc.
  2. marker_label     — name above the marker
  3. marker_battle    — battle id string (must exist in battleset JSON)
  4. marker_battleset — filename in datafiles/ (leave "" to use map default)
  5. marker_replay_pool — comma-separated battle ids for replays after clear

Marker states (sprites in Sprites / Map / mapMarker):

  locked    — Map_Marker_inactive  (dim blue crystal, 45% opacity)
  available — Map_Marker_orange    (orange crystal — current event)
  cleared   — Map_marker_Active    (bright blue crystal — done, can replay)

State is shown by the sprite only. Locked markers draw no label text.
Available/cleared markers show the event name above the icon when reachable.


5. Card rewards on markers (first clear)

Rewards use a **reward set**: how many cards to grant, whether to randomize, and a list of
{id, chance} entries. Configured in the marker Create event AFTER eventmarker_apply_config.

Only granted on **first clear** — replays give battles only, no cards.


--- Parameter reference ---

eventmarker_apply_reward(gift_count, randomize, rewardset?)

  gift_count   How many cards to grant (number of picks from the set)
  randomize    true = weighted roll each pick | false = walk entries in order
  rewardset    Optional — array or string "id:chance,..."; omit to use reward_add lines

eventmarker_reward_add(card_id, chance, collection?, once?)

  card_id      Card id from card_DB
  chance       Weight / percent (20 = 20% when entries sum to ~100)
  collection   Optional JSON collection name (only if ids overlap across files)
  once         true = one-time — after obtained, never rolls from any reward pool again


--- One-time reward ---

  eventmarker_apply_reward(1, true);
  eventmarker_reward_add(8, 100, "", true);

  String form: "8:100::once"  (4th field = once)
  JSON form:   { "id": 8, "chance": 100, "once": true }


--- Manual lines (recommended) ---

  eventmarker_apply_reward(2, true);
  eventmarker_reward_add(8, 20);
  eventmarker_reward_add(9, 10);
  eventmarker_reward_add(12, 70);

  Grants 2 cards. Each pick rolls 20% / 10% / 70% from the set.


--- Fixed card #7, once ---

  eventmarker_apply_reward(1, true);
  eventmarker_reward_add(7, 100);


--- One-liner string ---

  eventmarker_apply_reward(1, true, "8:20,9:80");


--- Array preset ---

  eventmarker_apply_reward(3, true, [
      { id: 1, chance: 25 },
      { id: 2, chance: 25 },
      { id: 3, chance: 25 },
      { id: 4, chance: 25 }
  ]);

  Grants 3 cards — each pick rolls independently from the weighted set.


--- String with optional collection ---

  eventmarker_apply_reward(1, true, "8:20:Merc_starterdeck01,9:80");

  Format per entry: id:chance or id:chance:collection_name


--- Non-random (grant in list order) ---

  eventmarker_apply_reward(2, false);
  eventmarker_reward_add(8, 100);
  eventmarker_reward_add(9, 100);

  First pick → card 8, second pick → card 9 (chance values ignored).


Rules:
  - Card ids must exist in card_DB.
  - gift_count = 0 or empty reward set → no card reward (battle-only marker).
  - randomize true → each of gift_count picks uses chance weights.
  - randomize false → picks follow entry order (0, 1, 2…).
  - First clear only. Replays use replay_pool battles, never grant cards again.


Full Marker01 example:

  event_inherited();
  eventmarker_apply_config(1, "Trail Start", "battle01", "Grasslands_Battleset01_starter.json", "battle01,battle02,battle03,battle04,battle05");
  eventmarker_apply_reward(1, true);
  eventmarker_reward_add(8, 100);


6. Room setup (Room_Worldmap1)

1. Open Room_Worldmap1 in the Room Editor.
2. Drag marker objects from Map1Markers into the room.
3. Position them anywhere on the map — order comes from marker_order, not X position.
4. OBJ_WorldMapController must exist in the room (calls worldmap_InitRoom on start).

You do NOT need to edit Grasslands_WorldMap01.json for normal setup.
Markers in the room are the source of truth. The JSON is only a fallback if a
marker field is left blank.


7. Battlesets (which enemies spawn)

File: datafiles/Grasslands_Battleset01_starter.json

Each battle lists waves of enemies by collection + enemyID:

  {
    "battle": "battle01",
    "active_slots": 1,
    "wave": [
      { "collection": "enemyCollection01", "enemyID": 1 },
      { "collection": "enemyCollection01", "enemyID": 1 }
    ]
  }

  battle01–battle08  = fight definitions (waves)
  enemyID            = must match an entry in EnemyCollection01.json

To point a marker at a fight, set marker_battle to "battle01" (or any battle id
in the battleset file named in marker_battleset).


8. Adding a new map 1 marker

1. In Asset Browser: right-click Map1Markers → Create Object
   (or duplicate OBJ_Map1_Marker05).
2. Set Parent Object to OBJ_EventMarker.
3. Open the new object's Create event and call eventmarker_apply_config(...).
4. Optional: add eventmarker_apply_reward(...) for a card on first clear.
5. Register the object in the project if you created it manually (IDE usually does this).
6. Place the instance in Room_Worldmap1.

Duplicate shortcut:
  Duplicate OBJ_Map1_Marker05 → rename to OBJ_Map1_Marker06 → edit Create event.


9. Starting map 2 (Map2Markers folder)

Same pattern as map 1:

1. Duplicate OBJ_Map2_Marker01 in Map2Markers.
2. Edit Create → eventmarker_apply_config(order, label, battle, battleset, replays).
3. Optional → eventmarker_apply_reward(gift_count, randomize) + eventmarker_reward_add lines.
4. Create Room_Worldmap2 (or duplicate Room_Worldmap1).
5. Place Map2 marker instances in that room.
6. Add OBJ_WorldMapController to the new room.
7. Optional: change which map config JSON loads in SCR_WorldMapController_Init.


10. Adding new monster JSON

Monsters are defined separately from battles and markers.

Step A — Define the enemy (stats, name, abilities)

  File: datafiles/EnemyCollection01.json

  Add a new entry under "enemy" with a unique enemyID:

  {
    "enemyID": 15,
    "enemyname": "Wolf",
    "type": "monster",
    "tag": ["Beast"],
    "sprite": "SPR_Monsterplaceholder",
    "animation": "GoblinSoldier",
    "level": 1,
    "enemyhealthvalue": 8,
    "enemyattackvalue": 5,
    "enemyability": [{ "type": "attack", "value": 5 }]
  }

  Ability types used in this project:
    attack, attack_all, heal, heal_all, self_buff, buff, destroy, silence

Step B — Use the enemy in a battle

  File: datafiles/Grasslands_Battleset01_starter.json

  Add or edit a battle wave entry:

  { "collection": "enemyCollection01", "enemyID": 15 }

  Then set a marker's marker_battle to that battle id.

Step C — Included Files (if new JSON file)

  If you create a NEW collection file (not EnemyCollection01.json):
  1. Put the file in datafiles/
  2. GameMaker → Included Files → add the JSON
  3. Open scripts/LoadMonsters/LoadMonsters.gml
  4. In SCR_LoadAllMonsters(), add:
       load_MonsterSet("YourNewCollection.json");

  For edits inside EnemyCollection01.json only — no loader change needed.
  Reload / restart the game so OBJ_GameController runs SCR_LoadAllMonsters() again.

Step D — New battleset file (optional)

  If map 2 uses different fights:
  1. Create datafiles/YourMap_Battleset.json (copy Grasslands_Battleset01_starter.json)
  2. Add to Included Files
  3. Set marker_battleset in the marker Create event to that filename

Step E — Add a new earnable player card (optional)

  1. Add the card to Merc_starterdeck01.json (or another loaded collection JSON).
  2. Omit "own" (or set own to 0) so the player does NOT start with it.
  3. Note the card "id" number.
  4. On a marker Create event, configure rewards with eventmarker_apply_reward + eventmarker_reward_add.
     See section 5 for examples.

Data chain summary:

  Card JSON (no own)        → card pool only (card_DB)
  Card JSON (own: N)        → starting player collection
  Marker Create event       → which battle + optional card reward
  Battleset JSON            → which enemyIDs spawn in each wave
  EnemyCollection JSON      → what those enemies actually are
